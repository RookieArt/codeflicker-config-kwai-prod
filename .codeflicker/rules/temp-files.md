# Temporary Files Rule

**Put all temporary files in `tmp/` directory.**

- Create `tmp/` directory if not exists.
- All temporary files (downloads, test outputs, scratch files) go into `tmp/`.
- Use `tmp/` as the working directory for transient operations.
- Do NOT pollute the project root with temporary artifacts.
